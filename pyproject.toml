[build-system]
requires = [
    "setuptools>=40.6.0",
    "wheel",
    "cffi>=1.12; platform_python_implementation != 'PyPy'",
    "setuptools-rust>=0.11.4",
]
build-backend = "setuptools.build_meta"

[tool.black]
line-length = 79
target-version = [
    "py36",
]

[tool.pytest.ini_options]
addopts = "-r s"
markers = [
    "requires_backend_interface: this test requires a specific backend interface",
    "skip_fips: this test is not executed in FIPS mode",
    "supported: parametrized test requiring only_if and skip_message",
    "wycheproof_tests: this test runs a wycheproof fixture",
]

[project]
version = "3.4.4+lineaje.1"
maintainers = [
    { name = "Lineaje DepFixer", email = "221268890+Lineaje-DepFixer@users.noreply.github.com" },
]

[project.urls]
Homepage = "https://github.com/lineaje-labs-gos/pyca-cryptography"
Repository = "https://github.com/lineaje-labs-gos/pyca-cryptography"
Tracker = "https://github.com/lineaje-labs-gos/pyca-cryptography/issues"
